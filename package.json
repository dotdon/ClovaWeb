{
  "name": "clova-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd clovalink && npm install && npm run build && node ../deploy.js"
  },
  "engines": {
    "node": ">=18.18.0"
  },
  "dependencies": {
    "fs-extra": "^11.2.0"
  }
}
